meta {
  name: show book
  type: http
  seq: 2
}

get {
  url: {{url}}/books/{{book_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 200
  res.body.id: eq {{book_id}}
}

settings {
  encodeUrl: true
}
