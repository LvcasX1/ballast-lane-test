meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{url}}/session
  body: json
  auth: inherit
}

body:json {
  {
    "email_address": "librarian1@library.com",
    "password": "password"
  }
}

headers {
  Content-Type: application/json
}

script:post-response {
  if (res.getStatus() === 200) {
    const responseData = res.getBody();
    bru.setEnvVar("token", responseData.auth_token);
  }
}

settings {
  encodeUrl: true
}
