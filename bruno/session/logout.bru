meta {
  name: logout
  type: http
  seq: 2
}

delete {
  url: {{url}}/session
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

script:post-response {
  if (res.getStatus() === 200) {
    bru.setEnvVar("token", "");
  }
}

settings {
  encodeUrl: true
}
