meta {
  name: login member
  type: http
  seq: 3
}

post {
  url: {{url}}/session
  body: json
  auth: inherit
}

body:json {
  {
    "email_address": "member1@example.com",
    "password": "password"
  }
}

headers {
  Content-Type: application/json
}

script:post-response {
  if (res.getStatus() === 200) {
    const responseData = res.getBody();
    bru.setEnvVar("token", responseData.auth_token);
  }
}

settings {
  encodeUrl: true
}
