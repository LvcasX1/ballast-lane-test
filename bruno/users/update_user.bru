meta {
  name: update user
  type: http
  seq: 5
}

patch {
  url: {{url}}/users/{{user_id}}
  body: json
  auth: inherit
}

body:json {
  {
    "user": {
      "name": "Updated Name"
    }
  }
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
}
