meta {
  name: show user
  type: http
  seq: 4
}

get {
  url: {{url}}/users/{{user_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 200
  res.body.id: eq {{user_id}}
}

settings {
  encodeUrl: true
}
